"Single Trace Extracter" shows individual data extractions from raw traces. Uses: duration_database
"Cell Analyzer" loads all txt's and handles the database information (e.g. types of drugs, how many recordgins, etc). It also helps plot/average data features for given cells. Uses: duration_database
"Data Analyzer" is essentially the data analysis results (Histograms, distributions, PCA, etc). It uses the "final data features database"
